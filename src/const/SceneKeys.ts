import { Scene } from "phaser";

enum SceneKeys {
  Preloader = "preloader",
  Waiting = "waiting",
  GameStart = "game-start",
  GameBackground = "game-background",
  GameOver = 'game-over'
}
export default SceneKeys;
